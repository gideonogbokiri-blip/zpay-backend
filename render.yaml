databases:
  - name: zpay-db
    plan: free
    databaseName: zpay
    user: zpay

services:
  - type: web
    name: zpay-backend
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        value: "*"
      - key: EMAIL_HOST
        value: smtp.gmail.com
      - key: EMAIL_PORT
        value: "465"
      - key: EMAIL_SECURE
        value: ssl
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
      - key: EMAIL_FROM
        value: "ZPAY <zpaycontact@gmail.com>"
      - key: DATABASE_URL
        fromDatabase:
          name: zpay-db
          property: connectionString